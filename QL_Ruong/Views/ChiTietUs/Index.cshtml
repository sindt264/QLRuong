@model IEnumerable<QL_Ruong.Models.ChiTietUs>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CTUSER_LEVEL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CTUSER_TRANGTHAI)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CTUSER_LEVEL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CTUSER_TRANGTHAI)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CTUSER_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CTUSER_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CTUSER_ID })
        </td>*@
    </tr>
}

</table>
